{
  // 如果es2016+用不了安装依赖后按住ctrl点击下面连接去修改lib里的es2016改成ESNext
  "extends": "@vue/tsconfig/tsconfig.web.json",
  "include": ["env.d.ts", "src/**/*", "src/**/*.vue"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  
  "references": [
    {
      "path": "./tsconfig.config.json"
    }
  ]
}
